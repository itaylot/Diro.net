# Next Step

Read `docs/PROJECT_STATE.md` and `docs/SESSION_LOG.md` (last entry only).

Then:
1. Identify the single most valuable next step based on current priority
2. State it clearly in one sentence
3. Explain why it's the right next step (2–3 sentences max)
4. List the specific files that would need to change
5. Estimate effort: small / medium / large

Do NOT start implementing.
Do NOT read code files unless the task is ambiguous.
Wait for the user to confirm before doing anything.

If multiple steps seem equally valid, present them as options and ask the user to choose.
