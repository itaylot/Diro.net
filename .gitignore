# Secrets
.env

# AI tooling — Claude Code config/commands, not part of the product
.claude/
CLAUDE.md
docs/SESSION_LOG.md

# Chrome profile (contains your Facebook login — never commit)
fb_chrome_profile/

# Logs
logs/
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/

# Personal runtime data — regenerated by the agents; contains your search
# results and choices, so keep it out of a public portfolio repo.
all_listings.json
seen_listings.json
seen_fb_posts.json
sent_telegram.json
dismissed.json
agent_status.json
agent_status.tmp
email_preview.html

# NOTE: settings.json IS committed (it's config), but secrets were moved to .env.
# tips.json IS committed (generic apartment tips — nice for the demo).
